[+] Возможность на один алиас повесить несколько алиасов.
[+] Возможность установки порядка обработки (алиасы, хистори, папки и т.д.)
[+] Защита паролем.

   TAR

•  По ^Enter запускать приложения в свернутом виде.
•  Закрывать  лоток  CD  автоматом, если к нему указан путь, а лоток не задвинут.
•  Поддержка переменных среды в команде "cd" (%TEMP% etc.).
•  Redo.  А  то  как-то  нехорошо,  Undo  есть, а вот когда обратное действие нужно сделать - облом.
•  Поддержка  переменных,  как путей при запуске из консоли (с автодополнением).

В диалоге выбора файлов хорошо бы добавить фильтр Только исполняемые (EXE, COM, BAT, PIF, LNK)

   tar_math

•  Пересчет  из  одной системы счисления в другую (DEC / HEX / BIN / OCT).
•  Генератор случайных чисел и алфавитно-циифровых комбинаций.
•  Подсчет процентов.
•  Конвертор валют.


Ну и вша по ходу дела - хотелось бы на странице с описанием плуга видеть все его команды, если таковые есть - было бы гораздо информативнее, и не пришлось бы лезть в «Edit Config», чтобы их просто посмотреть.

Очень нужно - автоматом вписывать ширину консоли в разрешение экрана, и если можно - задавать фиксированный отступ по краям.

Сделай пожалуйста отдельную настройку показа хинтов при прокрутке дропдауна.

Ты так и не пофиксил дополнение в случае если строка не отмечена (например, tb -> tbbeta).

Запуск файла с пробелом в пути не работает по «Shift»+»Enter», даже если его в кавычки поместить (Command Shell «cmd.exe», параметр «/k».

Неплохо бы, чтобы TAR понимал переменную среды %ComSpec% в качестве командного интерпретатора. ~~~ Работу со переменными я сделаю глобальной, ибо пока что только в некоторых случаях делается эта подстановка.

Хотелось бы менять порядок сортировки процессов в дропдауне «~killprocess».

Из концептуального:
1. Звуковое сопровождение (набора, ввода, результата);
2. Цветовые фильтры для всех типов строк;
3. Поиск во всех типах строк.

Из дополнений к существующему:
1. Отдельная  настройка  чувствительности  к регистру для всех
   типов строк;
3. Можно ли сделать раздельную настройку направления скроллинга
   для мыши и клавы?
4. Можно  ли  повесить открытие хистори на мышиный скроллинг с
   "Alt"?
5. Как  вариант  работы  "Trigger  show"  -  хочется  по "Esc"
   очищать консоль вместо ее закрытия;
6. Сейчас  при  внесении  в консоль строки текста, превышающей
   ее горизонтальные габариты, текст сдвигается влево и курсор
   находится   в   крайней  правой  позиции.  При  последующем
   удалении   текста  курсор  движется  направо.  Я  предлагаю
   автоматом  вписывать  строку в консоль при удалении текста,
   т.е. чтобы в процессе удаления курсор оставался на месте, а
   сама  строчка  "ехала"  налево до тех пор, пока ее длина не
   впишется  в  консоль и только после этого начинал двигаться
   направо сам курсор. Можно это сделать, как допфичу.
7. Сделай,  пожалуйста,  кнопку  "Apply" в настройках активной
   только при наличии каких-либо изменений.
9. Как   отключить   добавление  в  хистори  только  отдельных
   внутренних  команд  (а  не всех сразу) или отдельных команд
   плагинов (или только параметров для отдельных команд)?
10.  При  нажатии  "Apply"  в  настройках  элайс,  к  которому
   собственно  применяются  настройки прыгает на предпоследнюю
   снизу  позицию  в  списке  -  можно  сделать так, чтобы его
   положение в списке не менялось?

Плагины:

1. Дополнительные   команды   "~ras",   "~control",  "~mmc"  с
   автодополнением существующих аплетов/записей для tar_system.
2. Ты  не  в  курсе, случаем, как через rundll32.exe отключать
   removabe  drives и другую подключаемую по USB лабуду - тоже
   хотелось бы в tar_system, но если бы знал как это вызывать,
   пока обошелся бы :).
3. Может  добавишь в tar_system команду "~format"? Кстати, как
   туда    (rundll32    shell32,SHFormatDrive)   букву   диска
   передавать?
4. Что-то ты там собирался с поддержкой FTP в tar_urlcompleter
   делать.
5. Плагин для работы с UNC путями (с автодополнением).
